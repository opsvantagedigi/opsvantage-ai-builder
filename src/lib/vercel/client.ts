// Stub for Vercel client API
export const vercel = {
  addDomainToProject: async (vercelProjectId: string, domain: string) => {
    // Simulate API call
    return { success: true, vercelProjectId, domain };
  },
};
